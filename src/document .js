// first build card component 
// import robots from src  
/* but just note that robot is javaScript file with Array so when we import 
it we use curly brackets to excute and go through it */
// specify every element like 
// < card id = {robot[0].id} name= {robot[0].name}/>
// add props to arrow function 
// import card list component 
// move elements that renderd from index.js to cardlist then render card list 
/* use robot as property of cardlist component */
/* inside cardlist we had used map to select every element in robots array 
'we also should use a Key property to make 'i' unique'*/
/* Make a Search box */
/* first build app component to be a father of our webapp */
/* any class has to render and return something*/

